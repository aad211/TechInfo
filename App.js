/*
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 * @flow strict-local
 */

 //SEMENTARA DICOMMAND
/*import React, { Component } from 'react';
import { Button, StyleSheet, View, Text, Image, ScrollView, TextInput } from 'react-native';
import style from './src/style';
import fireAuth from './src/auth';
import dbService from './src/firestore';

import Home from './src/home';
import Nav from './src/nav';
import SignIn from './src/signIn';
import SignUp from './src/signUp';
import crud from './src/crud';
import listProduct from './src/listproduct';
import detailProduct from './src/detailproduct';
import { NativeRouter as Router, Route } from 'react-router-native';

export default class app extends Component {
  render() {
    return (
      <Router>
        <View style={{flex: 1}}>
          {/* Menu bar *}
          <View style={{flex: 0.9}}>
            <Route path="/" exact component={Home} />
            <Route path="/SignIn" component={SignIn} />
            <Route path="/SignUp" component={SignUp} />
            <Route path="/crud" component={crud} />
            <Route path="/listProduct/:id/:name" component={listProduct} />
            <Route path="/detailProduct/:id/:name/:picture/:detail" component={detailProduct} />
          </View>
          
          {/* Navigation bar *}
          <View style={{flex: .1}}>
            <Nav />
          </View>
        </View>
      </Router>
    );
  }
}*/